n 번쨰 원소부터

정수 리스트 num_list와 정수 n이 주어질 때,
n 번째 원소부터 마지막 원소까지의 모든 원소를 담은 리스트를 return하도록 solution 함수를 완성해주세요.


1. 동적 할당으로 공간을 생성하고 첫 번재 주소를 return 해야 합니다.
    => C는 배열의 길이를 동적으로 설정할 수 없습니다.
    => 먼저 알아내는 로직을 구합니다.
    => num_list_len은 정수 리스트이고 n의 시작점입니다.
    => for문에서 num_list_len, i = 0 / 전체를 돌리나 n-1가 같거나 초과하는 요소의 개수를 세서 length로 둡니다.


2. length를 통해 동적 할당


3. 1의 로직을 이용하여 동적 할당된 int * p에 값을 채워넣습니다.
    => 이 떄 for문의 i는 p의 i와 일치시면 안됩니다. -> 서로 다른 곳을 바라보고 있음
    => 지역변수 index를 사용해서 p에 두고 1의 for 내부 if문 로직에 적합하다면 값을 추가하고 index를 증가시킵니다.
