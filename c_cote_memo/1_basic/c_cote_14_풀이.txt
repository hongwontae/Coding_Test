배열에서 문자열 대소문자 변환하기

문자열 배열 strArr가 주어집니다. 모든 원소가 알파벳으로만 이루어져 있을 때, 
배열에서 홀수번째 인덱스의 문자열은 모든 문자를 대문자로, 짝수번째 인덱스의 문자열은 모든 문자를 소문자로 바꿔서 반환하는 solution 함수를 완성해 주세요.


1. 인자로 받은 const char * strArr []
    => strArr은 배열입니다.
    => strArr의 각 요소는 char 포인터입니다.
    => strArr은 첫 번쨰 요소의 주소 그 자체를 가지고 있습니다.


2. 정답은 ["..", "..."] 이 형태입니다.
    => 하나의 배열에 char 포인터가 존재하고 그 char 포인터가 첫 번쨰 문자 주소를 가져야 합니다.
    => 동적 할당을 통해 char *를 하나의 공간의 값으로 삼는 모양을 만들어야 합니다.
    => char ** pp = (char **) malloc (sizeof(char *) * n);
    => 그리고 나중에 pp[i]로 접근해서 동적할당 해야합니다. -> pp[i]는 pp[i]의 주소의 값 -> char *입니다. -> 요소의 값 그 자체입니다.


3. 이중 for문을 돌려야 합니다.
    => 이 떄 strArr 만큼 외부 for문
    => 문자열만큼 내부 for 문


4. for문에서 strcpy 처리, 널 문자 고려


5. 내부 for문은 length 만큼 돌리면 됩니다.
    => 애초에 널 문자를 고려하지 않아도 됩니다.


6. 짝수, 홀수 인덱스에 따라 값 변경
    => 그리고 이중 포인터 반환