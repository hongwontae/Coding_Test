정수 부분

실수 flo가 매개 변수로 주어질 때, flo의 정수 부분을 return하도록 solution 함수를 완성해주세요.


1. 실수의 정수 부분만 가져와야 합니다.
    => float을 int로 형 변환을 하면 소수점을 절삭하고 가져옵니다.
    => (int) flo -> 한 자리 n


2. 형 변환 뿐만 아니라 값의 타입을 설정하면 형이 변환됩니다.
    => int answer = flo;
    => flo가 int로 형변환 -> 소수점 절삭