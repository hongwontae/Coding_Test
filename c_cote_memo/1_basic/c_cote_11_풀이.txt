rny_string;

'm'과 "rn"이 모양이 비슷하게 생긴 점을 활용해 문자열에 장난을 하려고 합니다. 
문자열 rny_string이 주어질 때, rny_string의 모든 'm'을 "rn"으로 바꾼 문자열을 return 하는 solution 함수를 작성해 주세요.


** 문자열을 반환하면서 길이가 바뀌는 경우에는 항상 별도의 인덱스가 필요합니다. **
    => 이걸 망상해서 문제에 난항을 겪음


1. 문자 m을 rn으로 바꿔야 합니다.
    => 즉, 인자로 받은 문자열에 m의 개수를 세야 합니다.
    => 이 떄 for문 '\0' 조건문을 통해 알아냅니다.


2. 그리고 최종 문자열을 정의해야 합니다.
    => 이 떄 기존 문자열 개수 + m의 개수 + 1을 해야합니다.
=> m -> rn, 그래서 m의 개수 / 마지막 널 문자


3. 그리고 데이터를 집어넣을 for문을 돌립니다.
    3-1. 이 떄 rny_string의 널문자를 만날 때까지 돌립니다.
    3-2. 다만 분기에 따라 m을 만나면 index가 2개 증가하고 아니라면 1이 증가해야합니다.
    3-3. for 외부의 지역 변수 int index = 0;을 설정해서 도움을 받습니다.


4. 적용 후 널 문자를 추가한 후 반환합니다.