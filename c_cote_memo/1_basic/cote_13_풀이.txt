flag에 따라 다른 값 반환하기

두 정수 a, b와 boolean 변수 flag가 매개변수로 주어질 때, 
flag가 true면 a + b를 false면 a - b를 return 하는 solution 함수를 작성해 주세요.


1. #include <stdbool.h> 헤더를 가져오면 bool 자료형을 사용할 수 있습니다.
    1-1. bool a = true / bool b = false;
    1-2. bool kk = 100/ bool xxx = 0;
        => 0이라면 false / 나머지는 true


2. flag에 따라 a+b / a-b를 분기해서 return하면 됩니다.


3. 함수의 return 값이 전달되기 전에 타입은 함수 선언/정의에서 정해져 있습니다.
4. 함수의 return 값은 값이 복사되어 전달됩니다.