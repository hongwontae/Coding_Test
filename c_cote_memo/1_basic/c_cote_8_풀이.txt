뒤에서 5등까지

정수로 이루어진 리스트 num_list가 주어집니다.
num_list에서 가장 작은 5개의 수를 오름차순으로 담은 리스트를 return하도록 solution 함수를 완성해주세요.


1. 문제의 요지는 배열을 오름차순으로 정렬한 뒤 앞에서 5개만 가져오라는 이야기입니다.


2. malloc을 사용해서 5개 int를 만듭니다.


3. 이중 for문을 사용해서 인자로 받은 배열 첫 요소의 주소를 토대로 정렬합니다.
    => 이 떄 arr[0] 일 떄 arr[0]을 제외한 모두 비교 / +1 / arr[1]일 떄 arr[0], arr[1]을 제외한 모두 비교
    => 외부 for문은 초기값을 i=0 / 내부 for문은 j = i+1로 설정 -> 무조건 arr[1] / arr[2], arr[3].. 이렇게 비교됨
    => 그 떄 외부, 내부 for문 조건식은 index가 arr_length 이상이면 안됨 -> 알아서 조절됨


4. 이렇게 매개변수 arr를 해결하고 for문을 통해 p를 채워넣습니다.
